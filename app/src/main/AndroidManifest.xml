<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sauloaguiar.medicalconferences">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="com.sauloaguiar.medicalconferences.ConferencesApplication">
        <activity android:name="com.sauloaguiar.medicalconferences.conferences.view.ConferencesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".topics.view.TopicsActivity"/>
        <activity
            android:name=".conferences.view.ConferenceEditActivity"
            android:theme="@style/Dialog" />
    </application>

</manifest>